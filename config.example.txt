# Конфиг sibsutis-schedule.
#
# Скопируй этот файл в config.txt и впиши свои данные:
#     cp config.example.txt config.txt
#     chmod 600 config.txt
#
# config.txt в репозиторий не попадает (см. .gitignore) — учётные данные
# остаются только на твоей машине.
#
# Формат: key=value, по одной паре на строку. Строки с # — комментарии.

login=ваш_логин_от_my.sibsutis.ru
password=ваш_пароль

# Target по умолчанию — какое расписание тянуть без флагов и обновлять по таймеру.
# Задай РОВНО ОДИН из трёх ключей (или ни одного — тогда target указывается
# флагом --group / --teacher / --room при каждом запуске).
group=ИБ-211
# teacher=Иванов И.И.
# room=1-101

# --- Web-сервер (нужен только если запускаешь sibsutis-schedule-web) ---
# web_listen_addr=:8080                  # адрес и порт прослушивания
# cache_freshness_minutes=15             # как долго версия считается свежей

# Необязательно — переопределить адреса (по умолчанию значения ниже):
# schedule_url=https://my.sibsutis.ru/students/schedule/
# auth_url=https://my.sibsutis.ru/auth/
